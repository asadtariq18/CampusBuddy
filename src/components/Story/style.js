import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        

    },
    name: {
        textAlign: 'center',
        fontSize: 12,
        marginBottom: 5,

    }
});

export default styles;