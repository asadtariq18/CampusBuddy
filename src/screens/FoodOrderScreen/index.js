import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const FoodOrderScreen = () => (
  <SafeAreaView
    style={{backgroundColor: '#961010', justifyContent: 'center', height: '100%', }}>
            <Text style={{textAlign: 'center', fontSize: 40, color: '#ffffff'}}>Food Order</Text>
  </SafeAreaView>
);

export default FoodOrderScreen;
